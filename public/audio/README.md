Add custom music files in this folder, then register them in `src/music.ts`.

Example:

```ts
export const musicTracks = [
  {
    title: "Track title",
    artist: "Artist or note",
    src: "/audio/track-file.mp3",
  },
];
```
