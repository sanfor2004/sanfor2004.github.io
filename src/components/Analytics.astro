---
const id = (import.meta.env.PUBLIC_GA_MEASUREMENT_ID ?? "").trim();
const enabled = import.meta.env.PROD && /^G-[A-Z0-9]+$/.test(id);
---
{enabled && <meta name="sanfor-ga-id" content={id} />}
<script>
  import { initializeAnalytics } from "../lib/analytics";
  initializeAnalytics();
</script>
